/** Shared Recharts theme tokens (CSS vars set in index.css). */
export const chartTick = { fontSize: 11, fill: 'var(--chart-tick)' } as const
export const chartTickSm = { fontSize: 10, fill: 'var(--chart-tick)' } as const
export const chartTickXs = { fontSize: 9, fill: 'var(--chart-tick)' } as const
export const chartGrid = 'var(--chart-grid)'
export const chartSeriesNavy = 'var(--chart-series-navy)'
export const chartSeriesNavyAlt = 'var(--chart-series-navy-alt)'
export const chartLegendStyle = { color: 'var(--chart-legend)' } as const
